/**
 * Application route paths.
 * Centralizing paths here avoids magic strings and makes refactoring easier.
 */
export const PATHS = {
  HOME: "/",
  // Add other routes here as the app grows, e.g.,
  // PROJECTS: "/projects",
  // CONTACT: "/contact",
} as const;
